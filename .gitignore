node_modules/
.next/
dist/
build/
.cache/

# Generated design-token CJS shim (Plan 20-01) — emitted by
# scripts/build-design-tokens-cjs.ts on every predev/prebuild.
lib/design-tokens.cjs

# Plan 20-05 audit temp artifacts — read-only outputs from
# scripts/audit-patient-portal-tokens.ts (regenerable on demand).
.planning-tmp/
npm-debug.log*
pnpm-lock.yaml
yarn.lock

# OS / editor
.DS_Store
.vscode/
.idea/

# Env / secrets
.env
.env.*
!.env.example
tsconfig.tsbuildinfo

# Local Netlify folder
.netlify

# Supabase CLI local artifacts
supabase/.gitignore
supabase/config.toml
